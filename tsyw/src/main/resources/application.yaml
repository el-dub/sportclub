spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
  datasource:
    driver-class-name: org.postgresql.Driver
    url: "${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:6543/sportclub}"
    username: "${SPRING_DATASOURCE_USERNAME:postgres}"
    password: "${SPRING_DATASOURCE_PASSWORD:root}"

server:
  port: "${SERVER_PORT:8080}"

jwt.secret: ",lAMNQl(SAy^)k]|S 6w7#dyZC#,c`&vSR(^7Z0*pAPGkGS-Bq-|5)>F*-hvI?ym"